 <%= form_for :potential_user, :url => {:controller => "site", :action => "signup" } do |f| -%>

        We’ll e-mail you when we launch in your area.
        <div id="hrline"></div>
         
        <%= hidden_field_tag :from_facebook, facebook %>
       <p class="error">
         <% if@potential_user != nil && @potential_user.errors.any? %>

          <% @potential_user.errors.full_messages.each do |msg| %>
            <%= msg %><br/>
          <% end %>

        <% end %>
      </p>
      <table cellpadding="0" cellspacing="0" width="100%">
        <tr>
          <td class="label" align="left">
              Name:
          </td>
          
          <td align="right" style="padding-bottom:5px;">
            <%= f.text_field :name, :maxlength=>30,:class=>:textbox %>
          </td>
        </tr>
        <tr>
          <td class="label" align="left">
              Email:
          </td>
          <td align="right"  style="padding-bottom:5px;">
            <%= f.text_field :login, :maxlength=>40,:class=>:textbox %>

          </td>
        </tr>
        <tr>
          <td class="label" align="left">
              Area:
          </td>
          <td align="right"  style="padding-bottom:5px;">
   <span class="selectbox" ><%= f.select :area_id, options_for_select(Area.all(:order=>"name ASC").collect { |c| [c.name, c.id] }  ), :class=>"selectbox" %></span>

          </td>
        </tr>
        <tr>
          <td></td>
          <td>
            <p class="button">
              <%= submit_tag 'Submit' %>
            </p>
          </td>
        </tr>

      </table>      
      
      
<%end%>