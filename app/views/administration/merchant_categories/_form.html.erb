<%= form_for(@administration_merchant_category) do |f| %>
  <% if @administration_merchant_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@administration_merchant_category.errors.count, "error") %> prohibited this administration_merchant_category from being saved:</h2>

      <ul>
      <% @administration_merchant_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :maxlength=>50  %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
